#!/bin/bash

for OUTPUT in $(find /home/mhdgh/Desktop/my-development -type d | grep "node_modules")
do
	# echo $OUTPUT
	if [ -d "$OUTPUT" ]; then
	  # delete folder and continas folders
	  rm -dfr $OUTPUT;

	  echo ">>> done delete folder  ${OUTPUT}..."

	fi	
done
